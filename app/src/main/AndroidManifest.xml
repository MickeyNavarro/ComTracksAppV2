<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comtracksapp">

    <application
        android:name=".ListOfContacts"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContactProfile"></activity>
        <activity android:name=".AddBusinessContact" />
        <activity android:name=".AddPersonalContact" />
        <activity android:name=".EditBusinessContact" />
        <activity android:name=".Email" />
        <activity android:name=".Text" />
        <activity android:name=".Call" />
        <activity android:name=".EditPersonalContact" />
        <activity android:name=".activity_contact_profile" />
        <activity android:name=".AllContacts" />
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>